# 骑勇大战 · 回合制卡牌对战 DEMO 设计文档

> 状态：**已实现并可玩**。产物 `game.html`（单文件，双击即开）。
> 一键跑全流程：`node run.mjs`（模拟校准 → 构建 → 浏览器实测）
> 本文档记录 grilling 全过程的裁决结果与实现校准数据。🔒 已锁定，⚠️ 待验证，❗ 已知风险。
> 更新 2026-09-01

---

## 0. 一句话定位

2017 骑勇总决赛的 **1v1 斗将式卡牌对战**：三国杀的回合骨架 + 博德之门的 d20 判定 + 真实 NBA 战术相克。

**明确不是什么**：不是真实命中率模拟器（那是 `manager-sim` 项目的活），不是构筑类卡牌（唯一构筑环节是选教练）。

---

## 0.5 文件与命令

| 文件 | 作用 |
|---|---|
| `rules.mjs` | **规则内核，唯一真相源**。属性、牌表、相克矩阵、DC 计算、体能曲线 |
| `sim.mjs` | 校准模拟器，双方启发式 AI 对打 |
| `game.template.html` | 界面模板，含 `/*__RULES__*/` 注入点 |
| `build.mjs` | 把 `rules.mjs` 内联进模板 → `game.html` |
| `uitest.mjs` | 真实 Chrome 交互测试，**89 项断言** + 截图 |
| `playtest.mjs` | ⚠️ **自动对局机器人**，纯 DOM 点击打完整局，卡死即失败。交付前必过 |
| `synctest.mjs` | 实测 `file://` 下两窗口能否同步（结论：✅ 通，无需起服务器） |
| `run.mjs` | 一键跑全流程（sim → build → test → **play**） |
| `game.html` | **可玩产物**，单文件 84KB，零依赖零后端 |

```bash
node run.mjs            # 全流程
node run.mjs sim        # 只跑校准
node run.mjs build      # 只构建
node run.mjs test       # 只跑浏览器实测
```

**关键设计**：模拟校准与实际游戏**共用同一份 `rules.mjs`**，构建期内联。这让"校准出来的数值"与"实际玩到的数值"物理上不可能不一致。

### 玩法：两种都行

**一个人玩（推荐先这样试）**
1. 双击 `game.html`，选球队 + 双方教练 → 开始
2. 弹出引导页，点右边「**我一个人玩（用侧栏切视角）**」
3. 之后所有需要对方决策的地方，正文会出现「**切到 XXX 视角 →**」按钮，点它就行
4. 右上角侧栏也有「骑士 / 勇士」两个视角按钮，随时手动切；下面会提示「当前等 XXX 行动」

**真分屏对战（两个人 / 两块屏）**
1. 第一个窗口开局后，引导页点「**打开 XXX 窗口**」自动弹第二个窗口；若被浏览器拦截，页面上给了地址可点击复制
2. 或者手动开两个窗口：`game.html?side=CLE` 和 `game.html?side=GSW`
3. 两窗口通过 BroadcastChannel + localStorage 实时同步，**零后端**。
   已实测 `file://` 协议下同步正常（`synctest.mjs`：localStorage 跨窗口共享 ✅ / BroadcastChannel ✅），
   **不需要起本地服务器，双击 html 即可。**
4. 选人阶段互相看不到对方选了谁；技能全程公开

**注意**：不带 `?side=` 打开时，如果本机已有比赛在跑，会进入「**加入进行中的比赛**」界面让你选边，**不会冲掉原有比赛**。

---

## 0.8 校准实测（4000 局，全部达标）

| 指标 | 目标 | 实测 |
|---|---|---|
| 得分回合占比 | 46-50% | **45.6%** ✅ |
| 每回合得分 | 真实 NBA 1.10 | **1.098** ✅ |
| 一局回合数 | 14-30 | 中位 **16**，区间 9-26 ✅ |
| 用到球员数 | ≥3.5 / 5 | **4.91** ✅ |
| 双方胜率 | 差值 <16% | CLE **47.1%** / GSW **51.8%** ✅ |
| 命中率区间 | 约 15-75% | **15% - 80%** ✅ |
| 无人可派回合 | <5% | **0.00%** ✅ |
| 自然 20 / 自然 1 | 各约 5% | **5.2% / 5.3%** ✅ |
| 接管比赛触发 | 应可感知 | **0.91 次/局** ✅ |
| 结束原因 | 应以得分为主 | 得分 **95%**，牌堆见底 5% ✅ |

出手分布：三分 30.5% / 中投 20.8% / 突破 19.9% / 空接 13.7% / 扣篮 12.8%
防守应对：无 37.2% / 换防 15.7% / 延误 12.4% / 沉退 10.9% / 包夹 7.9% / 侧翼 6.5% / 缩防 6.3% / 全员 3.3%

### ❗ 两个让游戏"走流程"的 bug（重做布局时才发现）

**1. 防守方整局一张牌都摸不到。** `select` reducer 里只有进攻方摸牌，所以防守牌与应对牌用完就永远没有了。
→ 改为进攻方摸 2（进攻+战术），**防守方摸 1**（从 def/tac 里挑张数少的那堆）。

**2. 起手 37.8% 概率一张防守应对牌都没有。** 战术堆 24 张里应对 9 / 进攻战术 13 / 特殊 2，起手只发 2 张战术牌时经常一边倒。近四成开局有一方只能干看着。
→ 起手改为**保底各一张**（一张进攻战术 + 一张防守应对）。

修完的效果：

| 指标 | 修前 | 修后 |
|---|---|---|
| 防守应对为"无" | 52.9% | **37.2%** |
| 进攻战术为"无" | 43.8% | **30.0%** |
| 手上无进攻牌 | 4.34% | **2.27%** |
| 无人可派回合 | 0.70% | **0.00%** |

**真正发生博弈的回合从 47% 涨到 63%。** 这是"玩起来无聊"的根源——不是视觉问题，是一半回合防守方根本没牌可打。

### 调参过程中抓出的另外 4 个 bug

1. **模拟 AI 只看属性不看体能预算** → 52.68% 回合无人可派。前两轮把主力烧光。修：选人评分加入 `budgetLeft(p)*9`。
2. **手牌满 7 张时跳过摸牌** → 牌堆永不消耗，"牌堆见底"终局永不触发，比赛拖到体能耗尽后死锁。修：强制摸牌 + 回合末弃牌（照三国杀的弃牌阶段）。
3. **摸牌只从指定堆抽** → 该堆空了就抽不到，出现"剩 6 张牌但 deckTotal 永不为 0"的假见底。修：目标堆空则换非空堆。
4. **终局条件写成"两堆都空"** → 应为"任一方牌堆见底"。修正后 `noPlayer` 从 7.74% 降到 1.21%。

**方法论**：诊断脚本单独跑规则时失败率 0.00%，而模拟器里 37%——这个矛盾直接指向"bug 在模拟器不在规则"。**当两个实现同一规则的程序结果不一致时，先怀疑外层而不是规则本身。**

### 界面自查修掉的 5 处

| 问题 | 根因 |
|---|---|
| 骰子毫无分量（110px 小六边形） | 设计文档写了"d20 是情绪顶点应占屏"，实现时没做到。改 178px 真二十面体投影 + 翻滚跳数 940ms + 落地弹跳 |
| 球衣号码盖住球员名 | `.pk2` 内的 `<span>` 是行内元素堆不出块级布局。改 `display:flex` + `.pk2>*{display:block}` |
| 舞台中下部大片空白 | 日志固定 `max-height:150px`。改 `flex:1 1 auto` 填满剩余高度 |
| 选人选项挤成一行看不清 | 选人是每回合最重要的决策，改成 5 列大卡片网格，含体能条 / 属性 / 技能全文 |
| 空日志时半屏空洞 | 改为在空状态下显示规则速览，是真信息而非占位符 |

### 界面：三国杀式牌桌

```
┌──────────────────────────────────────────────────┐
│ GSW 比分   对面 5 张武将牌（体能格 / 属性 / 技能）      │
├──────────────────────────────┬───────────────────┤
│ 阶段 · 回合 · 视角切换 · 开窗口 │                   │
│ ┌────────┐  d20  ┌────────┐  │  对手技能全公开     │
│ │场上进攻人│      │场上防守人│  │  牌堆张数          │
│ └────────┘       └────────┘  │  教练体系          │
│ 桌面：本回合已打出的牌          │                   │
│ DC 拆解 chip（每一项修正来源）  │                   │
│ 提示 + 动作按钮                │                   │
│ 回合流 + 攻防相克速查表         │                   │
├──────────────────────────────┴───────────────────┤
│ CLE 比分   我方 5 张武将牌（点击派他上场）             │
├──────────────────────────────────────────────────┤
│ 手牌：进攻牌 / 防守牌 / 战术牌 —— 点亮的直接点击打出    │
└──────────────────────────────────────────────────┘
```

关键交互：
- **球员卡是竖版真卡牌**（291×152px）：号码大水印、球队渐变色条、上下分区（名字 / 体能格+属性 / 技能 / 页脚消耗），悬停抬起
- **选人 = 点自己那排球员卡**，卡上直接标体能剩余、已上几次、这次消耗几点
- **手牌是竖版扇形**（78×114px）：轻微旋转叠压、花色符号水印（◆进攻 ▲防守 ✦战术）、悬停抬起 18px
- **出牌 = 拖到高亮区域**（也保留点击）：拖起时合法落点自动点亮并显示"出手/干扰这次出手/打出战术/打出应对"，悬停加深，松手即出牌
- **进攻牌面直接印命中率** `需13 · 48%`（已含全部修正）
- **盲选期间对手整排 5 张全部翻面**（详见下节，这里有过一个真漏洞）
- **相克速查表常驻右栏**，当前生效的那行高亮

### 🔒 节奏优化：三处「多余操作」已删

Ethan 实测反馈 →「我可以不用打战术直接出进攻牌 / 防守方也可以不应对 / 应对完骰子就该开始判定 / 现在卡死在这个阶段双方不知道要干什么」。逐项处理：

**① 战术阶段进攻牌就可以打（跳过战术）**
`live.off = iAmOff && (phase==='shot' || phase==='tactic')`。但**不能跳过防守方的应对权**——那是他唯一的战术层决策。做法是把出手牌暂存到 `S.cur.pendingShot`，推进到 `respond`；对手应对结算完，`respond` reducer 末尾自动调 `REDUCERS.shot()` 出手（DC 用应对后的最新值算）。进攻方不会被问第二遍。

**② 掷骰改成自动**
原来 `roll` 阶段两边都渲染「掷 d20」按钮，双方都以为在等对方 → **看起来就是卡死**。而且防守方决定完干扰的那一刻，所有修正就已锁定，再点一下纯属多余。现在 `render()` 末尾检测 `phase==='roll'` 就自动触发 `rollWithAnim()`（含从对手窗口同步过来的情况），940ms 动画后落定。留一个「重掷（若卡住）」灰按钮兜底。

**③ 跳过应对 / 跳过干扰本来就支持**
`respond` 与 `defcard` reducer 在 `a.card` 为空时走「原始对位」/「不干扰」分支，界面上一直有对应按钮。这条是误判，无需改。

### 🔒 命中率标定：三次调整，最后一次是取向转变

**第一次（上限 20 → 18）**：Ethan 截图出现「需 20 以上 · 5%」问「这么难吗」。他是对的 —— `made = (roll >= dc)` 而掷 20 本就是自然 20 必进，所以 **DC 20 与 21 数学等价，是防守干扰牌的「一票否决」**。实测 11.5% 的组合被顶到 20。

**第二次（18 → 16）**：上限 18 的最低命中是 15%，但「≤15% 的组合占 25.7%」—— 四分之一的出手堆在下界。改成 16 对齐真实下界（Disney Research MIT Sloan 2014 追踪数据：防守人 0.6 秒内贴身的三分命中率 26.6%）。

**第三次（取向转变，这次是根本性的）**：Ethan 否掉了整个标定思路 ——

> 有防守人的情况也应该在 50~70% 至少 因为这个游戏大部分情况下是有对策牌的 不能让进攻效率那么差 反而应该高一点才好玩 所以有防守和无防守不能差太多 进攻牌也不能有缺的情况

**这是卡牌游戏，不是命中率模拟器。**「对齐真实 NBA」是 `manager-sim` 项目的目标，不是这里的。真实数据只在**相对排序**上有约束力（库里该比 TT 准），绝对值必须服从手感。

#### 关键数学发现：修正项跨度是硬约束

扫参数扫不出任何达标组合，去算才发现根因：

```
三分一项的修正总跨度 = 出手基准差 6 + 进攻属性 9 + 防守应对 5 + 干扰牌 4 + 防守属性 3 = 27 格 DC
而「45%~80%」目标区间在 d20 上只有 8 格
```

**无论怎么调基准和上下限都塞不进去。** 结果就是大量组合被封顶挤在边界，防守牌打不打都一样。

解法：加 `TUNING.modScale = 0.55`，`computeDC` 里所有修正项统一走 `mod()` 缩放。实测封顶率从 14.5% 降到 1.9%。

配套参数：`baseDC` 全体 -6（three 10 / mid 7 / drive 6 / dunk 4 / alley 5）、`dcFloor 3`、`dcCeil 14`、`homeEdge 0`（压缩后勇士优势已弱，再补偿骑士会反超到 55.7%）。

#### 连锁修正：胜利条件 11 → 15 分

命中率上去后每回合得分 1.10 → 1.67，一局从 16 回合压到 **11 回合** —— 技能冷却和体能预算全部失去意义。扫 targetScore：

| 目标分 | 一局回合数 | 用到球员数 |
|---|---|---|
| 11 | 11 | 4.60 |
| **15** | **16** | **4.97** |
| 18 | 19 | 5.00 |
| 21 | 22 | 5.00 |

选 **15 分** —— 回合数回到原来水平，且轮换机制真的在起作用。判据不是分数好看，而是**用到的单位数**。

#### 实测结果

| 场景 | 命中率 |
|---|---|
| 有防守应对 + 干扰牌 | 三分 50% / 中投·突破 60% / 扣篮·空接 70% |
| 手上 3 张牌的最优出手 | 中位 **70%**，<50% 仅占 0.9% |
| 空位无干扰 | 中位 75%，上限 90% |
| 唯一的 35% | **TT 投三分**（真 2K17 的 3PT 是 25，写实惩罚不是缺陷） |

另外 `select` reducer 加了**进攻牌保底**（摸完若 `hand.off` 为空则强制补一张），无进攻牌率 1.62% → **0.01%**。

⚠️ 改取向后 `sim.mjs` 的校准目标注释必须同步改（得分率 46-50% → 65-78%，命中率 15-75% → 45-90%），否则每次跑都误报 ❌。

### 🔒 交付前必过：自动对局机器人

**断言测试全绿 ≠ 游戏能玩。** 曾交付一个「88 项断言全绿但玩家操作两步就卡死」的版本，Ethan 原话：

> 你能不能先验证一下这个回合制的玩法是能玩的 然后再给到我啊 不然玩起来手感很差 而且很容易玩不明白 这个版本连我这个设计游戏的人都玩不明白 怎么给别人玩

根因：断言测试只走脚本设计好的那条路径，**走不到玩家真实操作序列的角落**。他踩的是「JR 埋伏 → TT 抢板 → 回到出手阶段」这个组合。

那次真卡死的代码级根因：`renderAction` 的 `shot` 阶段写成 `rows.length ? '' : A('放弃这次进攻', ...)` —— **兜底按钮是条件性的**。发动 JR 埋伏后 `jrAmbush` 标记置位使该按钮被 `benchActs` 过滤掉，此时若 `rows.length > 0` 则连"放弃"也没有，整个界面零可点项。

修法有三处：
1. 兜底按钮**无条件存在**（每个阶段都必须有一条出路）
2. `giveup` 改成独立 reducer（原来复用 `defcard` 会进掷骰阶段而 `cur.shot` 为 null → `SHOT_PTS[null]` = NaN）
3. `canRetry` 加 `hasShotCard` 条件（抢到板但无牌可投是纯空转）

新增 `playtest.mjs`：完全通过真实 DOM 点击打完整局，任何一步找不到可点击项就报卡死 + 打出最近 14 步轨迹。已接入 `run.mjs` 第四步。**实测 8/8 打完，0 卡死，0 页面错误。**

要点：不调 `__dispatch` 走后门（否则测不出界面缺按钮）；决策优先级为「选人 > 打手牌 > 实质按钮 > 切视角」且 `next` 排最后；给页面加 `window.__fastDice` 把骰子动画从 940ms 降到 30ms 否则超时。

### 🔒 盲选保密性：卡背必须是整排，不是单张

**只盖住被选中的那一张 = 用排除法把答案写在脸上。** 第一版就犯了这个错：`facedown` 只加给 `S.picks[sid]===p.id` 的那张牌，另外四张正常显示，玩家一眼就知道对手选了谁——盲选形同没有。

正确写法：`facedown = !S.revealed && !mySide`，盲选期间对手整排翻面，翻牌后整排恢复。

配套四条硬约束，每条对应一个真实泄漏渠道（全部有断言覆盖）：

| 泄漏渠道 | 修法 |
|---|---|
| `visibility:hidden` 只是视觉隐藏 | DOM 里直接不输出球员信息，翻面时只渲染空 div |
| `title` 属性悬停可读 | 翻面时不生成 title |
| `data-pid` 查看源码可读 | 翻面时不生成 data-pid |
| `.spent`（体能耗尽变灰）破坏五张一致性 | `.seat.facedown.spent{opacity:1;filter:none}` |

"对手是否已锁定"是**合法公开信息**（三国杀里也看得见对手在思考还是已出牌），但只能显示在队标区 `#oppLock`，绝不能挂在任何一张牌上。

对手体能与技能全程公开是设计意图（Q14 全明牌），且体能扣除发生在 `S.revealed = true` **之后**，所以它不泄漏当前选择。

### 拖放落点规则

| 拖的牌 | 阶段 | 落点 |
|---|---|---|
| 进攻牌 | shot（我是进攻方） | 中央进攻位大卡 |
| 防守牌 | defcard（我是防守方） | 中央防守位大卡 |
| 战术牌 | tactic（我是进攻方） | 桌面区 |
| 战术牌 | respond（我是防守方） | 桌面区 |

非法组合不响应 —— 玩家不用猜往哪拖。

---

## 1. 核心循环 🔒

```
双方同时秘密选人 → 同时翻牌
        ↓
进攻方：打战术牌（挡拆 / 牛角 / 无球跑动 …）
        或者 —— 直接打进攻牌，跳过战术这一步
        ↓
防守方应对（沉退 / 换防 / 包夹 / 延误 …），也可以「原始对位」不出牌
        ↓
进攻方看到应对后，再选进攻牌（三分 / 扣篮 / 两分 …）
        ↓
掷 d20 判定
        ↓
命中 → 得分，回合结束（一回合最多得一球）
不中 → 若有前场篮板类能力，可重掷；否则交换球权
```

### 为什么这个时序能同时满足两个矛盾的要求

Ethan 第一轮明确反对「猜进攻球员会怎么动」，但第五轮又要求「选人盲选、同时翻牌」。这两者靠**盲的层次**区分开：

- **盲只在选人这一层**（猜对手派谁）
- **翻牌之后全程明牌**（战术、应对、进攻牌依次公开，每一步都能看到上一步）
- 防守方的不确定性来自**他手里有没有那张应对牌**，而不是来自猜不到对手意图

这也解决了「后手必胜」悖论：防守方虽然后手可见，但手牌资源不足，**看得见不等于挡得住**。

### 举例（Ethan 原话的机制化）

1. 我选库里上场，出【挡拆】
2. 你如果出【沉退】→ 库里三分难度大降（真实篮球里 drop coverage 必须让出外线跳投）
3. 你如果出【无限换防】或【包夹】→ 库里三分难度上升，我只能改打别的进攻牌
4. → **我选库里的优势能不能发挥，取决于你手里有什么**

---

## 2. 场上结构 🔒

| 项 | 定义 |
|---|---|
| 名单 | 每方 5 人（原始需求"场上上 5 名球员"= 5 人名单，非 5 人同时在场） |
| 场上 | **每方只有 1 人**，完全对齐三国杀武将 |
| 技能 | 只能用当前上场那 1 人的技能 |
| 体能 | **只扣当前上场的那 1 人**，回合开始时扣 |
| 默认状态 | 1 对 1 |
| 多人动作 | 靠**翻牌召唤**实现（见下） |

### 翻牌召唤 🔒

需要第二个人的动作，由卡牌自带那个人：

- 【空接】→ 翻出内线球员来终结（借用其扣篮属性）
- 【包夹】→ 翻出第二名防守人
- 【挡拆】→ **抽象化，不需要指定掩护人**

被翻出的球员：
- **不算上场**
- **不触发其技能**
- **只借属性**
- 体能在回合开始时扣

> 设计理由：默认 1v1 让决策量最小；需要多人的动作自己带人来。这比"5 人常驻、每回合激活 1 人"干净得多。

### 错位天然产生 🔒

盲选下"我派库里防守 / 你翻出詹姆斯"本身就是错位，**不需要任何对位表机制**。
错位**只在当前回合有效，不延续**（Q33）。

---

## 3. 胜负与终局 🔒

| 项 | 值 |
|---|---|
| DEMO 胜利条件 | **先得 15 分** |
| 硬天花板 | 牌堆见底 → 分高者胜 |
| 单回合得分上限 | **1 球**（前场篮板给的是"再掷一次骰"的重试权，进球即刻结束回合） |

**为什么是 15 分**：原定 11 分是按「每回合 1.10 分」（真实 NBA）算的，那时一局 16 回合。命中率重标定后每回合 1.67 分，11 分只剩 **11 回合** —— 技能冷却和体能预算全部失去意义。扫 targetScore 后选 15 分，回合数回到 16，用到球员数 4.97/5。

**校准目标**（已随取向转变更新）：
- 得分回合占比 **65-78%**（原为 46-50%，那是按真实 FG% .471 反推的）
- 每回合得分 **1.4-1.9**（真实 NBA 是 1.10，本项目刻意高于它）
- 命中率区间 **45-90%**（有防守的局面也要 50-70%）

---

## 4. 骰子系统 🔒

### 基本式

```
d20 + 修正值 ≥ DC  →  成功
```

- 自然 20 = 打进加罚（and-one）
- 自然 1 = 失误，直接丢球权
- 战术只给**优势/劣势**（掷两颗取高 / 取低），不改骰子面数

### ❗ 关键决策：放大命中率差异，放弃统计学真实

真实数据（Disney Research MIT Sloan 2014 追踪论文，逐字）：

> If a defender is more than 6ft away shooting percentage 40%. Average = 36.7%. If a defender is closer than 6ft at release shooting percentage is 32%. If a defender is closer than 6ft, 0.6 secs before release shooting percentage drops to 26.6%.

ESPN SportVU 交叉印证（勇士常规赛，按最近防守人距离）：`0-2ft 35.3% / 2-4ft 35.8% / 4-6ft 40.3% / 6+ft 42.0%`

换算 d20：

| 情境 | 真实命中率 | d20 需要 |
|---|---|---|
| 完全空位 | 43% | 12 |
| 联盟平均 | 36.7% | 14 |
| 防守人贴近 | 32% | 15 |
| 起跳前被扑到 | 26.6% | 16 |

**从毫无防守到被死死封盖，全部区间只有 4 格。** 技能修正压在 ±3 以内玩家根本感觉不到。

**决策**：把命中率区间从真实的 27-43% 放大到约 **15-75%**，保留相对排序（空位仍比被防好，库里仍比 TT 准）。

**代价（必须接受）**：本作**不得宣称"真实模拟 NBA 命中率"**。它是"篮球感觉正确"，不是"篮球数据正确"。忠于 36.7% 那件事归 `manager-sim` 项目 —— 两个项目在此分工。

⚠️ 具体 DC 表需空跑 50 局校准，目标是"约一半回合得分"。

---

## 5. 牌堆系统 🔒

### 三堆分开抽

| 牌堆 | 内容 | 三国杀对应 |
|---|---|---|
| 进攻堆 | 三分 / 扣篮 / 两分 / 上篮 / 空接 … | 【杀】 |
| 防守堆 | 封盖 / 抢断 / 干扰 … | 【闪】 |
| 战术堆 | 挡拆 / 牛角 / 无球跑动 / 沉退 / 换防 / 包夹 / 延误 … | 锦囊 |

**摸牌时自选从哪堆摸**（比如每回合摸 2 张，可以两张都摸战术，也可以各摸一张）。

解决的问题：
- 纯随机会导致进攻牌积压打不出去（Ethan 明确担忧）
- 让**摸牌本身成为决策**
- 配合"存牌"玩法：可以攒一套组合（挡拆 + 空接 + 反制），等体能满时一次打出

| 项 | 值 |
|---|---|
| 手牌上限 | **固定 7 张**，与体能脱钩 |
| 终局 | 总牌数见底 |
| 起始手牌 | ⚠️ 待定，需按最终牌表算期望 |
| 每回合摸牌数 | ⚠️ 待定，同上 |

### ❗ 手牌上限为什么不锚在体能上（推翻三国杀原设计）

三国杀"手牌上限 = 当前体力"能成立，是因为它是**减法游戏**：血掉光就死，下行螺旋有终点。

本作是**加法游戏**，若手牌锚在体能上会产生无终点的恶性螺旋：
```
体能低 → 被迫弃牌 → 更没牌打 → 更打不出分 → 更落后
```
落后方会越来越无力且永远不结束。故固定 7 张，同时充当"存牌空间"。

---

## 6. 体能系统 🔒

### 体能点数 = round(2017 总决赛 G4 真实出场分钟 / 7)

数据源：ESPN G4 box score `espn.com.au/nba/boxscore/_/gameId/400954513`

| 骑士 | 分钟 | 点 | | 勇士 | 分钟 | 点 |
|---|---|---|---|---|---|---|
| LeBron James | 41 | **6** | | Kevin Durant | 39 | **6** |
| Kyrie Irving | 41 | **6** | | Draymond Green | 39 | **6** |
| Tristan Thompson | 36 | **5** | | Stephen Curry | 38 | **5** |
| Kevin Love | 29 | **4** | | Klay Thompson | 34 | **5** |
| J.R. Smith | 29 | **4** | | Andre Iguodala | 21 | **3** |
| **合计** | | **25** | | **合计** | | **25** |

**双方总量刚好相等，各 3 个档位。**

### ❗ 为什么不用 2K 的 Stamina 属性

已抓到真实数据（`nba2k-player-ratings/data/finals2017_stamina.json`）：

| 球员 | Stamina | | 球员 | Stamina |
|---|---|---|---|---|
| 詹姆斯 | 98 | | 库里 | 98 |
| 欧文 | 97 | | 杜兰特 | 98 |
| 勒夫 | 93 | | 克莱 | 91 |
| JR | 92 | | 格林 | 88 |
| TT | 90 | | 伊戈达拉 | 80 |

线性映射 80-98 → 3-6 点的实测结果（`verify-stamina.mjs`）：
- **骑士 27 点 vs 勇士 24 点** —— 白送骑士 12% 资源优势
- 骑士内部只有 6/6/5/5/5 两档 —— "每人不同"落空

根因：**2K 的 Stamina 表达"疲劳速度"而非"能打多久"**，所以顶级球员全是 97-98（都不容易累），区分度天然小。而出场时间是**结果**，已把 Stamina、伤病、教练信任、角色定位全部综合进去。

> Stamina 数据不浪费 —— 留给"体能低于一半时的手感衰减速度"（Stamina 高的人衰减慢），那才是它真正表达的东西。

### 消耗曲线（按本局累计上场次数递增）

| 第 n 次上场 | 1 | 2 | 3 | 4 | 5 | 6+ |
|---|---|---|---|---|---|---|
| 消耗 | 1 | 1 | 1 | 2 | 2 | 3 |

**恢复**：每连续歇满 2 个回合恢复 1 点（不超过上限）。恢复**不重置**已用次数。

各球员最大上场次数（`verify-stamina-v2.mjs` 实测）：詹姆斯/欧文/TT/杜兰特/格林/库里/克莱 = 4 次；勒夫/JR/伊戈达拉 = 3 次。

### ❗ 为什么是"累计次数"而不是"连续次数"

最初方案是"连续上场消耗递增"，**被代码验证证伪**：

| 策略 | 结果 |
|---|---|
| 只用詹姆斯一人 | 只能上 3 次（1+2+3=6 耗尽），17/20 回合无人可派 |
| 詹姆斯/欧文**交替** | **完全绕过递增惩罚**（每次对本人都"不连续"，永远只花 1 点），各上 6 次，TT/JR/勒夫全程未上场 |

惩罚打在"连打"这个表象上，而最优策略根本不连打 —— 交替就行。这是个能被两行代码绕过的规则。

改成"按累计次数"后实测（20 回合需求）：

| 策略 | 派不出人 | 用到几人 |
|---|---|---|
| 只用前两人交替 | **8 / 20** | 2 人 |
| 贪心派最强 | 0 / 20 | **4 人** |
| 均衡轮换 | 0 / 20 | **5 人** |

**结论：想打满 20 回合，必须用到至少 4 个人。** 二人组包办被彻底堵死。

> 篮球语义上也对：打了很多分钟的人就是会累，跟他是不是连续打的无关。
> 体能的含义因此清晰化：**体能不是"能上几次"，而是"上场预算"**。

### 验证脚本

- `verify-stamina.mjs` —— 旧方案（Stamina 映射 + 连续递增）的证伪记录
- `verify-stamina-v2.mjs` —— 现行方案验证，改参数可直接重跑

---

## 7. 技能系统 🔒

### 三类技能

| 类别 | 冷却 | 例 |
|---|---|---|
| ① 需要冷却 | 有 | 大部分主动技能 |
| ② 不需要冷却 | 无 | 触发式被动 |
| ③ 全场只能用一次 | — | 詹姆斯"进球续打" |

**冷却计数单位 = 自己的进攻回合数**（双方各进攻一次 = 一轮）。

### 场下响应技能

部分球员拥有可在**不上场时发动**的技能，代价是消耗其体能。

> 这解决了 JR Smith 与 Iguodala 的设计难题：他们的历史定位本就是"从替补席站出来的人"，场下响应比主动技能更写实（Iguodala 2015 FMVP 正是如此）。
> 与"翻牌召唤"分工清晰：**翻牌借属性，场下响应用技能**，两者不重叠。

### 詹姆斯"接管比赛" 🔒

- 前一回合判定进球 → 下一回合进攻**可以继续上，不消耗体能**
- **全场只能触发一次**，连打结束后不能再次连打
- 免费续打的那一回合，**防守方可以重新盲选**（否则对手只能干看着）
- 属于第 ③ 类技能，不占冷却

> 这是全场唯一能打破"轮换压力"的技能，因此价值极高，也正是"接管比赛"该有的重量。

### 10 人技能（已实现，`rules.mjs` `ROSTERS`）

体能点数 = round(G4 出场分钟/7)。属性主干取真 2K17 的 OVR/3PT/DNK，其余维度按角色手工设计。

**骑士**

| 球员 | 体能 | 三分/中投/突破/扣篮/空接 | 外防/内防/抢断 | 技能 | 类型 |
|---|---|---|---|---|---|
| 詹姆斯 23 | 6 | 76/80/95/85/92 | 82/78/72 | **接管比赛**：本方上一回合进球后，本回合可让詹姆斯上场且不消耗体能 | 全场一次 |
| 欧文 2 | 6 | 84/88/92/30/80 | 58/40/55 | **单挑无解**：打出【单打】时突破与中投 DC -4；但本回合失去重试机会 | 冷却 2 |
| 勒夫 0 | 4 | 84/74/55/55/62 | 52/66/48 | **高位外弹**：对手应对为【沉退】或【包夹】时，三分 DC 额外 -4 | 常驻 |
| JR 5 | 4 | **89**/70/66/70/64 | 70/45/62 | **弱侧埋伏**：对手应对为【包夹】或【侧翼封堵】时，可从场下接手本回合出手，三分 DC -3 | 场下可用 |
| TT 13 | 5 | **25**/35/58/75/78 | 62/82/42 | **二次进攻**：出手不中时掷骰争抢前场篮板（DC 12），成功则获得一次重试；在场下可花 1 体能发动 | 常驻 |

**勇士**

| 球员 | 体能 | 三分/中投/突破/扣篮/空接 | 外防/内防/抢断 | 技能 | 类型 |
|---|---|---|---|---|---|
| 杜兰特 35 | 6 | 90/92/86/75/88 | 80/74/62 | **错位死刑**：当防守人外围防守低于 85 时，中投与三分 DC -5 | 冷却 1 |
| 格林 23 | 6 | 80/62/66/74/72 | **88**/86/82 | **守门员**：在场时对手扣篮与空接 DC +4；在场下可花 1 体能对本回合发动一次 | 常驻 |
| 库里 30 | 5 | **99**/90/82/36/78 | 62/34/70 | **无限射程**：防守应对对库里三分的 DC 加成永不超过 +2 | 常驻 |
| 克莱 11 | 5 | **98**/84/68/65/70 | 82/52/58 | **接球就投**：打出【无球跑动】或【手递手】后三分 DC -5；不能作为【单打】执行人 | 常驻 |
| 伊戈达拉 9 | **3** | 80/72/78/80/78 | **90**/66/76 | **死亡缠绕**：对手判定前可从场下接防，本回合对手所有出手 DC +5 | 场下 2 次 |

**JR 与伊戈达拉的难题怎么解的**：他们的历史特色是"偶尔爆发"和"防守+关键球"，都难做成主动技能。解法是**场下响应** —— 他们本就是"从替补席站出来的人"（伊戈达拉 2015 年 FMVP 正是如此）。伊戈达拉 3 点体能只能上 3 次，逼着他的价值必须来自场下，这跟历史完全对上。

---

## 8. 战术相克表（技能设计骨架）🔒

Ethan 硬要求原话：「**你得清楚知道 nba 的战术直接的针对和反制才能设计出平衡且真实的技能**」

来源：`joinstriveon.com/blog/pick-and-roll-basketball`（Coverage Cheat Sheet）、`nbaplaybook.com/?p=2864`、`thebenchviewbasketball.com/id/panduan/pick-and-roll-defense-complete-guide`

| 防守应对 | **必须让出什么** | 进攻反制 |
|---|---|---|
| **Drop 沉退** | 中距离拉杆跳投 | 挡拆后直接跳投；大个 pick-and-pop 外弹三分 |
| **Hedge/Show 延误** | 短挡拆 4 打 3 | 口袋传球给顺下/外弹的大个 |
| **Switch 换防** | 错位（大防小 / 小防大） | 立刻单打错位人；re-screen 制造更好错位 |
| **Ice/Down 侧翼封堵** | 被逼边线死角、弱侧空 | 拒绝挡拆走中路；skip pass 转移弱侧 |
| **Blitz/Trap 包夹** | 后场 4 打 3 | 穿越包夹的口袋传球，顺下人打 4 打 3 |

### 关键洞察

**每一种防守都不是"减少命中率"，而是"强迫你换一种得分方式"。** Drop 不是让你投不进，是逼你投中距离而不是上篮。

### 二阶反制（高级技能素材）

- 进攻 **slip**（假掩护提前切入）专治 hedge / switch
- 防守 **scram switch** 反制 slip 与错位
- 进攻 **ghost screen** 制造犹豫
- 防守对 pop 的铁律："if he pops, you stay"

### 防守机制分两层 🔒

| 层 | 牌 | 机制 |
|---|---|---|
| 即时反应层 | 封盖 / 抢断 | **降 DC**（对应【闪】） |
| 战术层 | 沉退 / 换防 / 包夹 / 延误 | **改变哪类进攻牌好用**（对应锦囊） |

只有降 DC 的话，格林和 JR 的防守差异只剩数值大小，"技能有特色"就废了。
只有改可用牌的话，"封盖库里三分"这个最常见的一对一对抗没地方表达。

### 防守方无应对牌时 🔒

= 按对位球员的**原始防守属性**算 DC，不加成不减成。

> 防守牌的定位是**额外的干扰手段，不是防守本身**。这保证即使一张牌都没有，格林防守也还是比欧文好 —— 球员差异永远在场。
> 反面方案（无牌 = DC 大降）会让手牌运气直接变成比分，连续三回合没抓到防守牌就连丢三球，最挫败。

---

## 9. 教练牌 🔒

赛前选定，**整局不可换**（与球员每回合可换形成对比）。这是本作**唯一的构筑环节**。

### 机制 = 改牌堆构成 + 解锁专属战术牌

教练在真实篮球里做的事**就是决定你能打什么套路**，所以"改牌堆 + 解锁专属牌"是最贴的映射。牌堆一旦构建好就不能改，这跟教练不能换是同一件事。

不采用的方案：改摸牌数（教练退化成数值调节器）、给全队被动（隐形，打完一局不知道教练干了什么）。

### 三位教练的真实体系（已核实）

**Mike D'Antoni = 快节奏 + 全员换防** ✅
- "seven seconds or less"，2017 年火箭 55-27，当选 2017 年度最佳教练
- 火箭换防口号逐字：「**Talk it. Touch it. Switch it. Grab it.**」
- 德安东尼原话："We would like to keep our matchups, but if a guy gets screened and he can't get through and control his man, we switch it"
- 来源 `houstonchronicle.com/sports/rockets/article/Rockets-have-learned-new-lines-of-defense-12353772.php`

**Steve Kerr = 传切 + 无球跑动** ✅
- 具体战术：`"4" Pop Fist`（Klay 底角穿掩护清空一侧 → 中锋 DHO 给格林 → 库里给 KD 下掩护）、`Motion Weak PNR`（控卫过半场立刻出球 → 无球跑到弱侧 → 重新接球打挡拆）、`Loop Punch Stagger Split`（Klay 绕掩护 Loop → West 低位背身 → Iguodala 靠拢形成双掩护 → 最弱一环 Ian Clark 从双掩护间空切得分）
- 科尔复出首场勇士全队 **34 次助攻**
- 有趣事实：**科尔的 "4" Action 就是从德安东尼太阳队搬来的**，两个体系有共享血统

**❗ Tyronn Lue ≠ 巨星单打体系（Ethan 原判断错误，已纠正）**

SF Chronicle 逐字：
> **both Kerr and Lue scrapped offenses that were big on isolation plays for the stars** (James and Kyrie Irving; Curry and Klay Thompson) but left the other guys kinda standing around. **Lue's offense, like Kerr's, stresses constant movement and touches by everyone.**

单打恰恰是**科尔想逼骑士打的东西**。Mercury News 逐字：`Kerr is setting up his rotation... to force LeBron to beat the Warriors defense in isolation`

**卢的真实招牌 = 用掩护逼中锋换防到詹姆斯身上（猎杀错位）**：
> Lue exploited the Warriors' centers repeatedly by getting either Zaza Pachulia, David West or JaVale McGee to **switch on LeBron by sending his center for a screen**, which regularly produced a freebie shot for LeBron

**猎杀库里的真实数据**（`goldengatesports.com/2017/06/16/...`）：G1 高位挡拆，若由**詹姆斯**发起且库里防掩护人 → 9 次进攻得 10 分（**1.11 PPP**）；若由**欧文**发起 → 5 次进攻仅得 4 分（**0.8 PPP**）。骑士的掩护人常是 **JR Smith**，目的就是把库里拽出来。

科尔的反制：让 **Klay 单防欧文** + **格林当"守门员"**（原文 `Draymond Green acting as a goalie`），逼欧文 G3-G4 改变打法。

> 这个纠正对设计有利：卢的真实体系（制造错位 → 猎杀）天然对上德安东尼的"无限换防"（换防制造错位），这就是要的真实相克。

⚠️ 教练牌具体解锁哪几张，待战术牌清单定稿后填。

---

## 10. PVP 与技术实现 🔒

| 项 | 决策 |
|---|---|
| 模式 | 分屏 PVP，Ethan 自己开两个窗口打自己 |
| 手牌 | 双方**看不到对面手牌** |
| 技能 | **可以查看对面技能**（因此技能必须一句话说清） |
| 同步 | `BroadcastChannel` 或 localStorage `storage` 事件，**零后端** |
| 不做 | AI 对手、热座、联网 |

### 视觉基线（与球星卡应用保持连贯）

沿用 `F:\nbacard\nba_cards_v7\auction.html` 的设计令牌（单文件 vanilla HTML，无框架无构建）：

```
--ink: #070a13        底色，渐变到 #0a0e1a / #05070d
--gold: #f2b33d       主金
--gold-hi: #ffd97a    高光
--gold-2: #d9962a     暗部
--text: #edf0f8       文字
--muted: #8a90a6      次级
--faint: #5a6076      最弱
品质六档 白绿蓝紫金红: #c9ccd6 #3ecf6e #3d8bff #a05cf5 #f5c451 #ef3b56
--font-display: "Anton", "Arial Narrow Bold", "PingFang SC", "Microsoft YaHei", sans-serif
--font-ui: "Manrope", -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif
--font-num: "Bahnschrift", "DIN Alternate", "Segoe UI", Consolas, monospace  + tabular-nums
圆角 16/12/8    --ease-spring: cubic-bezier(0.34, 1.4, 0.44, 1)
全局 opacity .05 + mix-blend-mode overlay 的 SVG feTurbulence 噪点 + 两束顶部 conic 射灯
卡牌 3D 翻牌结构已有：.card-front / .card-back / .card-back-face / .card-inner
```

**注意**：球星卡应用**没有骰子系统**（它的随机性是 PSA 磨损倍数 + beta 分布采样 + 三层保底）。骰子是本作全新的视觉语言，需要新造。d20 那颗骰子应该慢、占屏、有音效 —— 这是全场情绪顶点，值得单独做丢弃式原型定手感。

---

## 11. 数据源现状

### ❗ 真 2K17 分项数据不存在（已彻底验证，勿再尝试）

| 尝试 | 结果 |
|---|---|
| `2kratings.com/teams/2016-17-golden-state-warriors` | 存在，35 项齐全，**但 title 是 `NBA 2K27 Roster`** = 2K27 对经典阵容的重制值。证据：该页 Curry OVR=96，真 2K17 是 94；副标题写 `Year(s) in the NBA: 17` |
| `2kratings.com/teams/2016-17-cleveland-cavaliers` | **HTTP 404，页面不存在**。骑士只能退到 `2015-16-cleveland-cavaliers` |
| `#nav-2k17-table` 归档区 | 真 2K17 数据，**但只有 OVR / 3PT / DNK 三列**，缺其余 32 项 |
| `nba2kw.com/nba-2k17-ratings` | 404 |
| `2kdb.net` | 只到 2K20 |
| Kaggle `nba_2017_*.csv` | 是球场统计，非 2K 属性 |
| `hoopshype.com/nba-2k/players/?game=nba-2k17` | 仅 OVR |
| `bbs.eyeuc.com/down/view/32245` | 唯一真源，需游戏本体 + REDitor 扫内存 |

另外：**2K17 属性 schema 与 2K27 不同**（2K17 有 Standing/Moving Mid、Boxout、Potential，无 Close Shot、Help Defense IQ）→ 无法字段对齐。

### 决策：只用真 2K17 的 30 个数字当锚点，其余手工设计

**真 2K17 三列（可直接硬编码）**

| 骑士 | OVR/3PT/DNK | 勇士 | OVR/3PT/DNK |
|---|---|---|---|
| LeBron James | 96 / 76 / 85 | Stephen Curry | 94 / **99** / 36 |
| Kyrie Irving | 89 / 84 / 30 | Kevin Durant | 93 / 90 / 75 |
| Kevin Love | 82 / 84 / 55 | Klay Thompson | 90 / **98** / 65 |
| Tristan Thompson | 81 / **25** / 75 | Draymond Green | 90 / 80 / 74 |
| J.R. Smith | 77 / **89** / 70 | Andre Iguodala | 81 / 80 / 80 |

值得注意：
- **JR 的 3PT 89 比欧文的 84 还高** —— 这就是他的技能定位来源
- **TT 的 3PT 只有 25** —— 印证了"不需要禁止规则，数值自己会惩罚"（TT 可以打三分牌，只是 DC 高到离谱）
- Iguodala 80/80/80 极均衡

理由：demo 只需要 10 人 × 6-8 个派生维度（三分/终结/传球/外防/内防/篮板/体能），不是 35 项。这 30 个真数字已覆盖最关键的三个维度。而且既然已决定放大命中率差异，无论用哪套 2K 原值都要重新缩放，35 项精度在这里是浪费。

### ❗ 阵容的历史准确性

- **骑士五人是 2017 总决赛真实首发** ✅（詹姆斯 / 欧文 / 勒夫 / JR / TT，源 ESPN G4 box score）
- **勇士"死亡五小"从未以首发身份对上骑士首发** ❗ 2017 总决赛勇士真实首发是 **Zaza Pachulia**（G4 首发 16 分钟），Iguodala 是替补（21 分钟）。死亡五小是收官阵容。
- → 这是个**合理的 fantasy 对位**，但**不能标"2017 总决赛复刻"**。

### 抓取器复用坑

复用 `nba2k-player-ratings/src/scrape.js` 抓经典阵容时新增了 `--checkpoint=` 与 `--no-export` 开关。**必须用独立 checkpoint 文件**：最终导出按 `name` 去重，而 "Stephen Curry" 在 2016-17 页与现役页同名，混跑会**静默丢行并污染现役数据集**。

另：Iguodala 页面的 `Agility` 字段本身渲染为 `--`（站点缺数据），scrape.js 因强制 35 项齐全而判其失败。其余 34 项正常。

---

## 12. 待办

已全部完成并校准，剩下的是可选扩展：

| # | 事项 | 状态 |
|---|---|---|
| 1 | 三个牌堆的完整牌表 | ✅ 已实现（`rules.mjs` `buildDecks`），总量约 30 张/方，保证 25-30 回合内抽完 |
| 2 | 起始手牌 7 张 + 每回合摸 2 张 | ✅ 已校准 |
| 3 | DC 表 | ✅ 已校准到 47.2% 得分率 |
| 4 | 10 人技能定稿 | ✅ 全部一句话卡面，嵌进相克表 |
| 5 | 教练牌解锁清单 | ✅ 三教练各改牌堆构成 + 解锁专属牌 |
| 6 | d20 骰子手感 | ✅ 178px 二十面体 + 翻滚跳数 + 落地弹跳 |

### 可选扩展（未做）

- **音效**：骰子落地、命中、自然 20 各一个音。BG3 那颗骰子的分量一半来自声音。
- **AI 对手**：`sim.mjs` 里的启发式 AI 已经能打（会算期望分、会顾体能预算），接到界面上即可单人游玩。
- **完整犯规系统**：目前只有自然 20 的 and-one。犯规累计 + 罚球 + 6 次离场值得做，因为"故意犯规送 TT 上罚球线"是骑勇系列真实存在的博弈。
- **更多阵容**：`ROSTERS` 结构可直接扩展，属性来源见第 11 节。
- **21 分制**：`TUNING.targetScore` 改成 21 即可，一局约 30 回合。

### 明确不问、只能靠跑的东西

- 摸 2 张是否正确 —— 牌堆构成一变这个数就得变
- 挡拆该 +2 还是 +5 —— 空跑看得分分布
- 骰子动画多慢才有分量 —— 手感问题，扔原型比讨论快

---

## 附录：被推翻的设计（避免走回头路）

| 轮次 | 曾经的方案 | 为什么废弃 |
|---|---|---|
| 1 | 球员即卡牌，无牌库无抽牌 | Ethan 改要三国杀骨架 |
| 1 | 3 个威胁点 vs 2 个应对点（明牌资源博弈） | 被【杀】→【闪】反应式结构取代 |
| 1 | 战术模板决定点亮哪 3 个位置 | 战术改成手牌 |
| 1 | 单页同屏全明牌 | 改分屏隐藏手牌 |
| 3 | 衰减同时挂"体能 + 对手适应"双通道 | Ethan 只要挂球员 |
| 3 | 通用【无懈可击】反制牌 | 改成战术间的具体相克 |
| 4 | 固定回合数上限 | Ethan 第一轮就选了竞速 |
| 5 | 错位延续到下一回合 + 对位表 | Ethan 要只在当前回合有效；1v1 下错位天然产生 |
| 6 | 5 人常驻场上、每回合激活 1 人 | Ethan 的"翻牌召唤"方案更干净 |
| 7 | 体能完全不恢复 | Ethan 要保留恢复 |
| 7 | 詹姆斯无限连续续打 | Ethan 限定全场一次 |
| 7 | 手牌上限锚在体能上（照抄三国杀） | 加法游戏会产生无终点的恶性螺旋 |
| 8 | 体能点数用 2K Stamina 线性映射 | 代码验证：骑士 27 vs 勇士 24 不平衡，且档位不足 |
| 8 | 连续上场消耗递增 | 代码验证：交替使用可完全绕过 |

### 我在过程中犯的两个错（记录以免重犯）

1. **算体能供给时忽略了 cap**：我称"每歇 1 回合恢复 1 点会产生 100 点供给"，忽略恢复不能超上限，真实约 43 点。**教训：算资源供给必须考虑 cap，不能把线性外推当真实供给。**
2. **判断 1v1 会让挡拆/包夹/换防失去语义**：Ethan 用"翻牌召唤"绕过了，比我的 5 人常驻方案更好。**教训：结构限制常有卡牌层面的解法，不一定要改结构。**
