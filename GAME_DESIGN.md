# 骑勇大战 · 回合制卡牌对战 —— 游戏设计文档

> 2017 骑勇总决赛的 1v1 斗将式卡牌对战。
> 三国杀的回合骨架 + 博德之门的 d20 判定 + 真实 NBA 战术相克。
>
> 本文档是**游戏设计说明书**：讲清楚这游戏是什么、怎么玩、为什么这么设计。
> 开发期的决策记录与踩坑见 `DESIGN.md`，快速上手见 `README.md`。

---

## 目录

1. [设计目标](#1-设计目标)
2. [核心设计思路](#2-核心设计思路三个支柱)
3. [玩法规则](#3-玩法规则)
4. [数值系统](#4-数值系统)
5. [技能系统](#5-技能系统)
6. [牌表与相克矩阵](#6-牌表与相克矩阵)
7. [教练系统](#7-教练系统)
8. [技术架构](#8-技术架构)
9. [校准数据](#9-校准数据)
10. [验证流程](#10-验证流程)

---

## 1. 设计目标

### 1.1 一句话定位

让**懂球的人**在牌桌上认出真实篮球的博弈：你出的不是"攻击力 5"，而是"沉退防守"；你知道沉退必须让出外线跳投，所以你期待对手打挡拆外弹，而你要赌他手里没有那张牌。

### 1.2 是 / 不是

| 是 | 不是 |
|---|---|
| 回合制卡牌对战 | 真实命中率模拟器（那是 `manager-sim` 项目的活） |
| 每回合 1v1 斗将 | 5v5 全场模拟 |
| 战术相克的博弈 | 卡组构筑（唯一构筑环节是选教练） |
| 有 d20 随机性的策略游戏 | 纯运气掷骰 |

### 1.3 三条硬约束

1. **盲选只在选人这一层** —— 猜对手派谁，不猜他要怎么打
2. **翻牌后全程明牌** —— 战术、应对、进攻牌依次公开，每步都能看到上一步
3. **看得见不等于挡得住** —— 防守方后手可见，但受限于手牌资源

> 这三条同时解决了两个矛盾要求：既要"选人盲选"的悬念，又要"不靠猜对手意图"的公平。

---

## 2. 核心设计思路（三个支柱）

### 支柱一：防守不是"降低命中率"，而是"强迫你换一种得分方式"

这是整个设计的基石，来自真实挡拆防守相克表：

> **Drop（沉退）不是让你投不进，是逼你投中距离而不是上篮。**

所以每种防守应对都有明确的"必须让出什么"，进攻方的应对就是打那个让出的位置。这让**防守选择变成了一种承诺**——你选沉退，就等于宣布"我放你外线，但你别想突破"。

### 支柱二：防守分两层，缺一层技能就废了

| 层 | 牌 | 机制 | 类比 |
|---|---|---|---|
| 战术层 | 沉退 / 换防 / 包夹 / 延误 … | **改变哪类进攻牌好用** | 三国杀的锦囊 |
| 即时反应层 | 干扰投篮 / 封盖 / 抢断 / 紧逼 | **改变 DC** | 三国杀的闪 |

- 只有即时层 → 格林和 JR 的防守差异只剩数值大小，"技能有特色"就废了
- 只有战术层 → "封盖库里三分"这个最常见的一对一对抗没地方表达

### 支柱三：牌是资源，不是能力

防守方即使**一张牌都不出**，格林防守也还是比欧文好（按原始防守属性算 DC）。

> 防守牌的定位是**额外的干扰手段，不是防守本身**。这保证球员差异永远在场，牌是增量而不是替代品。

---

## 3. 玩法规则

### 3.1 完整回合流程

```
① 双方同时秘密选人（盲选）
        ↓ 同时翻牌
② 摸牌：进攻方 1 进攻牌 + 1 战术牌，防守方 1 防守牌 + 1 战术牌
        ↓
③ 进攻方：打战术牌  ——或——  直接打进攻牌跳过战术
        ↓
④ 防守方应对：打出应对牌  ——或——  「原始对位」不出牌
        ↓
⑤ 进攻方选定出手方式（三分 / 中投 / 突破 / 扣篮 / 空接）
        ↓
⑥ 防守方决定是否花干扰牌
        ↓
⑦ d20 判定（自动掷）
        ↓
⑧ 结果：命中 → 得分，回合结束
        不中 → TT 可拼前场篮板换一次重试；否则交换球权
        ↓
⑨ 交换球权，回到 ①
```

**关键时序**：进攻方先出战术，防守方看到后才应对。这个顺序让防守方的知识优势转化为"我恰好有这张牌吗"的资源博弈，而不是"我猜对了"的运气博弈。

### 3.2 场上结构

| 项 | 定义 |
|---|---|
| 名单 | 每方 5 人 |
| 场上 | **每方只有 1 人**（完全对齐三国杀武将） |
| 技能 | 只能用当前上场那 1 人的技能 |
| 体能 | 只扣当前上场的那 1 人 |

**翻牌召唤**：需要第二个人的动作由卡牌自带那个人。

- 【空接】→ 翻出内线球员来终结（借用其扣篮属性）
- 【包夹】→ 翻出第二名防守人
- 【挡拆】→ 抽象化，不需要指定掩护人

被翻出的球员：**不算上场、不触发技能、只借属性**。

> 设计理由：默认 1v1 让决策量最小；需要多人的动作自己带人来。这比"5 人常驻、每回合激活 1 人"干净得多。

**错位天然产生**：盲选下"我派库里防守 / 你翻出詹姆斯"本身就是错位，不需要任何对位表机制。错位只在当前回合有效，不延续。

### 3.3 盲选保密性

**对手整排 5 张全部翻面，不是只盖被选中的那一张。**

> 只盖住被选中的那张 = 用排除法把答案写在脸上。第一版就犯了这个错：另外四张正常显示，玩家一眼就知道对手选了谁。

对应断言（缺一不可）：

```js
seats.filter(s => s.classList.contains('facedown')).length === 5   // 整排都翻
seats.filter(s => s.getAttribute('data-pid')).length === 0         // 无 id
new Set(seats.map(s => s.className)).size === 1                    // 5 张视觉全同
```

### 3.4 摸牌与手牌

| 项 | 值 |
|---|---|
| 每回合摸牌 | 进攻方 1 进攻 + 1 战术；防守方 1 防守 + 1 战术 |
| 起手 | 8 张（3 攻 / 3 防 / 2 战） |
| 手牌上限 | 11 |
| 保底 | 某类手牌为空时强制补一张 |

> **"起手 ≥ 上限"是致命配置**。早期起手 7 张 + 上限 7 张，起手就是满的，每回合摸 2 张立刻触发弃牌（摸了又扔），玩家实际只有 1 张进攻牌可选。留 3 张囤牌空间才有"存着用"的决策余地。

弃牌策略：弃数量最多的那类，但**保证每类至少留 2 张**（否则会留空类）。

### 3.5 判定

```
掷 d20 ≥ DC  →  命中
自然 20      →  打进加罚（and-one）
自然 1       →  失误，直接丢球权
```

**自动掷骰**：防守方决定完干扰的那一刻，所有修正就已锁定，再点一下"掷 d20"是多余操作。骰子自动落定，留一个"重掷"灰色按钮兜底。

### 3.6 胜负

| 项 | 值 |
|---|---|
| 胜利条件 | **先得 15 分** |
| 硬天花板 | 牌堆见底 → 分高者胜 |
| 单回合得分上限 | **1 球**（前场篮板给的是重试权，进球即刻结束回合） |

> **为什么是 15 分**：原定 11 分按"每回合 1.10 分"算，那时一局 16 回合。命中率重标定后每回合 1.67 分，11 分只剩 **11 回合**——技能冷却和体能预算全部失去意义。扫 `targetScore` 后选 15 分，回合数回到 17，用到球员数 4.97/5。

### 3.7 体能系统

**体能点数 = round(2017 总决赛 G4 真实出场分钟 / 7)**

| 球员 | 出场分钟 | 体能 | 可上场次数 |
|---|---|---|---|
| 詹姆斯 | 41 | 6 | 4 |
| 欧文 | 41 | 6 | 4 |
| 杜兰特 | 39 | 6 | 4 |
| 格林 | 39 | 6 | 4 |
| 库里 | 38 | 5 | 4 |
| TT | 36 | 5 | 4 |
| 克莱 | 34 | 5 | 4 |
| 勒夫 | 29 | 4 | 3 |
| JR | 29 | 4 | 3 |
| 伊戈达拉 | 21 | 3 | 3 |

> 可上场次数由 `maxUsesFor(sta)` 算出，不是简单的 `体能 ÷ 1`——因为消耗随累计次数递增（1/1/1/2/2/3…），6 点体能只能上 4 次而不是 6 次。

**消耗曲线**（按本局**累计**上场次数递增）：

```js
function costOfNthUse(n) { return n <= 3 ? 1 : n <= 5 ? 2 : 3; }
```

> **为什么是"累计次数"而不是"连续次数"**：连续次数会被交替使用绕过（A-B-A-B 永远只花第 1 次的代价）。累计次数让"轮换"成为真实约束。

**休息恢复**：每歇 2 个回合回 1 点体能，上限为初始体能值。

**疲劳惩罚**：体能低于一半后开始衰减出手 DC，衰减速度由 2K Stamina 决定（耐力好的人衰减慢）：

```js
function fatiguePenalty(p, cur) {
  const half = p.sta / 2;
  if (cur > half) return 0;
  const missing = half - cur + 1;
  const tough = (STAMINA_2K[p.id] ?? 90) / 98;
  return Math.round(missing * TUNING.fatigueStep * (2 - tough));
}
```

> **为什么不用 2K 的 Stamina 当体能点数**：Stamina 在 2K 里衡量的是"在场上能跑多久"，在本作里一次上场只打一个回合，用它当点数是错配。它在这里的正确用法是**决定疲劳衰减速度**。

---

## 4. 数值系统

### 4.1 DC 计算公式

DC（Difficulty Class）是"这次出手有多难"。掷 d20 ≥ DC 即命中。

```
DC = 出手类型基准
   + 进攻属性修正   (属性每高于 50 一点，DC 降低)
   + 防守属性修正   (属性每高于 50 一点，DC 升高)
   + 防守应对修正   (沉退/换防/包夹… 对这类出手)
   + 进攻战术修正   (挡拆/单打/无球跑动… 对这类出手)
   + 二阶反制修正   (教科书惩罚)
   + 防守干扰牌修正 (干扰投篮/封盖/抢断/紧逼)
   + 技能修正
   + 体能衰减
   然后钳制到 [dcFloor, dcCeil]
```

**所有修正项统一乘以 `modScale`（0.55）**：

```js
const mod = (label, v) => {
  const s = Math.round(v * TUNING.modScale);
  if (!s) return;
  parts.push({ label, v: s });   // parts 用于界面摊开每一项给玩家看
  dc += s;
};
```

> **为什么必须缩放**：修正项的原始跨度太大——三分一项的总跨度是 **27 格 DC**（出手基准差 6 + 进攻属性 9 + 防守应对 5 + 干扰牌 4 + 防守属性 3），而"45%~80%"这个目标区间在 d20 上只有 **8 格**。不缩放的话无论怎么调基准和上下限都塞不进去，结果就是大量组合被封顶挤在边界，防守牌打不打都一样。

### 4.2 关键参数

| 参数 | 值 | 说明 |
|---|---|---|
| `baseDC` | 三分 10 / 中投 7 / 突破 6 / 扣篮 4 / 空接 5 | 出手类型基准 |
| `modScale` | 0.55 | 所有修正项统一缩放 |
| `attrScale` | 0.115 | 进攻属性每高于 50 一点 |
| `defScale` | 0.068 | 防守属性每高于 50 一点 |
| `dcFloor` | 3 | 最高命中 90%（再好的机会也不是必进） |
| `dcCeil` | 14 | 最低命中 35% |
| `stealDC` | 15 | 抢断成功难度（约 30%） |
| `reboundDC` | 12 | 前场篮板争抢难度 |
| `targetScore` | 15 | 胜利分数 |

### 4.3 命中率标定：一次取向转变

**这是设计上最重要的一次转向。**

初版按"对齐真实 NBA 追踪数据"标定（贴身三分 26.6%）。但实测反馈推翻了这个取向：

> 「有防守人的情况也应该在 50~70% 至少，因为这个游戏大部分情况下是有对策牌的，不能让进攻效率那么差，反而应该高一点才好玩，所以有防守和无防守不能差太多。」

**这是卡牌游戏，不是命中率模拟器。** 大多数局面双方都有对策牌，进攻效率太低会让出牌决策失去意义。真实数据只在**相对排序**上有约束力（库里该比 TT 准），绝对值必须服从手感。

调整后的实测：

| 场景 | 命中率 |
|---|---|
| 有防守应对 + 干扰牌 | 三分 50% / 中投·突破 60% / 扣篮·空接 70% |
| 手上 3 张牌的最优出手 | 中位 **70%**，低于 50% 只占 0.9% |
| 空位无干扰 | 中位 75%，上限 90% |
| 唯一的 35% | **TT 投三分**（真 2K17 的 3PT 是 25，写实惩罚不是缺陷） |

### 4.4 DC 上限的四次调整史

| 上限 | 最低命中 | 问题 |
|---|---|---|
| 20 | 5% | `made = roll >= dc` 而掷 20 本就是自然 20 必进，所以 **DC 20 与 21 数学等价**，是防守干扰牌的"一票否决"。实测 11.5% 的组合被顶到 20 |
| 18 | 15% | "≤15% 的组合占 25.7%"，四分之一的出手堆在下界 |
| 16 | 25% | 对齐真实下界，但取向本身错了 |
| **14** | **35%** | 配合 `modScale` 0.55，让"有防守"落在 50-70% |

> **通用教训**：`Math.min(骰面数, dc)` 这种硬编码上限要跟判定式 `roll >= dc` 一起看。`dc === 面数` 时该出手就死了。上限应设为 **面数 - 2** 以上。

---

## 5. 技能系统

### 5.1 三类技能

| 类别 | 冷却 | 例 |
|---|---|---|
| ① 需要冷却 | 有（按自己的进攻回合计） | 杜兰特错位死刑（CD 1）、欧文单挑无解（CD 2） |
| ② 常驻被动 | 无 | 库里无限射程、克莱接球就投 |
| ③ 全场一次 | — | 詹姆斯接管比赛 |

### 5.2 场下响应技能

部分球员可在**不上场时**发动技能，代价是消耗其体能。

> 这解决了 JR 与伊戈达拉的设计难题：他们的历史定位本就是"从替补席站出来的人"（伊戈达拉 2015 FMVP 正是如此），场下响应比主动技能更写实。
>
> 与"翻牌召唤"分工清晰：**翻牌借属性，场下响应用技能**，两者不重叠。

伊戈达拉只有 3 点体能（只能上 3 次），逼着他的价值必须来自场下——这跟历史完全对上。

### 5.3 骑士（CLE）

| 球员 | 体能 | 三分/中投/突破/扣篮/空接 | 外防/内防/抢断 | 技能 | 类型 |
|---|---|---|---|---|---|
| **詹姆斯** 23 | 6 | 76/80/**95**/85/92 | 82/78/72 | **接管比赛**：本方上一回合进球后，本回合可上场且不消耗体能 | 全场一次 |
| **欧文** 2 | 6 | 84/88/92/**30**/80 | 58/40/55 | **单挑无解**：打出【单打】时突破与中投 DC -4；但本回合失去重试机会 | 冷却 2 |
| **勒夫** 0 | 4 | 84/74/55/55/62 | 52/66/48 | **高位外弹**：对手应对为【沉退】或【包夹】时，三分 DC 额外 -4 | 常驻 |
| **JR** 5 | 4 | **89**/70/66/70/64 | 70/45/62 | **弱侧埋伏**：对手应对为【包夹】或【侧翼封堵】时可从场下接手出手，三分 DC -3 | 场下（花 1 体能） |
| **TT** 13 | 5 | **25**/35/58/75/78 | 62/**82**/42 | **二次进攻**：出手不中时掷骰拼前场篮板（DC 12），成功获一次重试；在场下可花 1 体能发动 | 常驻 |

### 5.4 勇士（GSW）

| 球员 | 体能 | 三分/中投/突破/扣篮/空接 | 外防/内防/抢断 | 技能 | 类型 |
|---|---|---|---|---|---|
| **杜兰特** 35 | 6 | 90/**92**/86/75/88 | 80/74/62 | **错位死刑**：防守人外防 < 85 时，中投与三分 DC -5 | 冷却 1 |
| **格林** 23 | 6 | 80/62/66/74/72 | **88**/86/82 | **守门员**：在场时对手扣篮与空接 DC +4；在场下可花 1 体能发动一次 | 常驻 |
| **库里** 30 | 5 | **99**/90/82/36/78 | 62/34/70 | **无限射程**：防守应对对库里三分的 DC 加成永不超过 +2 | 常驻 |
| **克莱** 11 | 5 | **98**/84/68/65/70 | 82/52/58 | **接球就投**：打出【无球跑动】或【手递手】后三分 DC -5；不能作为【单打】执行人 | 常驻 |
| **伊戈达拉** 9 | **3** | 80/72/78/80/78 | **90**/66/76 | **死亡缠绕**：判定前可从场下接防，本回合对手所有出手 DC +5 | 场下（全场 2 次） |

### 5.5 詹姆斯"接管比赛"的特别说明

- 前一回合判定进球 → 下一回合进攻**可以继续上，不消耗体能**
- **全场只能触发一次**
- 免费续打的那一回合，**防守方可以重新盲选**（否则对手只能干看着）

> 这是全场唯一能打破"轮换压力"的技能，因此价值极高——也正因如此它只能触发一次。

---

## 6. 牌表与相克矩阵

### 6.1 牌堆构成

按"一局最长约 21 回合"反推（中位 17，P95 约 21）。

**进攻堆（19 张）**：三分 ×5、中投 ×4、突破 ×4、扣篮 ×3、空接 ×3

**防守堆（16 张）**：干扰投篮 ×5、封盖 ×5、抢断 ×3、紧逼 ×3

**战术堆（基础 27 张）**：挡拆 3 / 沉退 3 / 延误 3 / 无限换防 3 / 牛角 2 / 无球跑动 2 / 单打 2 / 手递手 2 / 侧翼封堵 2 / 包夹 2 / 缩防换位 1 / 点哥战术 1 / 教练暂停 1

战术堆会再按教练调整（±1~2）并加入 2 张专属解锁牌，**三种教练下都是 31 张**。三堆合计 **66 张/方**。

> 早期防守堆只有 8 张（3 种），打到后半局必然枯竭——这是"打到后面能出的牌少得可怜"的直接原因。

### 6.2 防守应对（战术层）

依据真实挡拆防守相克表（Coverage Cheat Sheet / nbaplaybook）。

| 应对 | 三分 | 中投 | 突破 | 扣篮 | 空接 | **必须让出** |
|---|---|---|---|---|---|---|
| 沉退 | +1 | +1 | +4 | +6 | +6 | 外线跳投与大个外弹 |
| 延误 | +4 | +3 | +4 | +1 | +0 | 短挡拆顺下 4 打 3 |
| 无限换防 | +5 | +3 | +1 | +1 | +2 | 错位单打 |
| 侧翼封堵 | +2 | +5 | +3 | +3 | +3 | 弱侧底角三分 |
| 包夹 | +3 | +6 | +5 | +1 | +0 | 后场 4 打 3 |
| 缩防换位 | +2 | +2 | +4 | +4 | +4 | 外围出手 |
| 全员换防 | +3 | +3 | +3 | +3 | +3 | 无（德安东尼专属，免疫错位惩罚） |
| 原始对位 | 0 | 0 | 0 | 0 | 0 | 一切 |

### 6.3 进攻战术

| 战术 | 三分 | 中投 | 突破 | 扣篮 | 空接 |
|---|---|---|---|---|---|
| 挡拆 | -3 | -3 | -2 | -1 | -2 |
| 牛角 | -2 | -3 | -1 | -3 | -3 |
| 无球跑动 | **-5** | -2 | 0 | 0 | 0 |
| 单打 | 0 | -3 | **-4** | -2 | 0 |
| 手递手 | -4 | -2 | -1 | 0 | 0 |
| 分球空切（科尔） | -4 | -2 | -1 | 0 | -1 |
| 中锋上提（卢） | -1 | -2 | -3 | -2 | -2 |

### 6.4 二阶反制（教科书惩罚）

当"进攻战术 × 出手类型 × 防守应对"三者匹配时触发额外修正：

| 战术 | 出手 | 被反制的应对 | 修正 | 说明 |
|---|---|---|---|---|
| 挡拆 | 三分 | 沉退 | -3 | 挡拆外弹惩罚沉退 |
| 挡拆 | 中投 | 沉退 | -3 | 沉退让出拉杆跳投 |
| 牛角 | 三分 | 沉退 | -3 | 双高位外弹惩罚沉退 |
| 挡拆 | 空接 | 延误 | -3 | 口袋传球穿延误 |
| 牛角 | 空接 | 延误 | -3 | 短挡拆 4 打 3 |
| 挡拆 | 空接 | 包夹 | -4 | 穿越包夹的口袋传球 |
| 牛角 | 空接 | 包夹 | -4 | 包夹后场 4 打 3 |
| 挡拆 | 扣篮 | 包夹 | -3 | 包夹后顺下终结 |
| 单打 | 突破 | 无限换防 | -3 | 直接单打错位 |
| 单打 | 中投 | 无限换防 | -3 | 错位背身 |
| 无球跑动 | 三分 | 侧翼封堵 | -3 | 弱侧转移 |
| 单打 | 突破 | 侧翼封堵 | -3 | 拒绝挡拆走中路 |
| 挡拆 | 中投 | 缩防换位 | -2 | re-screen 再打一次 |
| 无球跑动 | 三分 | 缩防换位 | -2 | 缩防后外线空档 |

### 6.5 防守干扰牌（即时反应层）

**四种牌必须有四种不同的用途**，不能只是数值大小的差别。

| 牌 | 三分 | 中投 | 突破 | 扣篮 | 空接 | 特殊效果 | 定位 |
|---|---|---|---|---|---|---|---|
| **干扰投篮** | +4 | +4 | +1 | 0 | 0 | — | 克跳投 |
| **封盖** | 0 | +1 | +3 | +5 | +5 | — | 克内线 |
| **抢断** | +1 | +1 | +1 | +1 | +1 | **掷 d20 ≥ 15 直接断球** | 赌断球 |
| **紧逼** | +2 | +2 | +2 | +2 | +2 | **额外消耗对手 1 点体能** | 耗体能 |

> 早期版本三种牌全是纯 DC 修正矩阵，玩家只看到"+X 难度"。而且 `canSteal` 和 `stealDC` **定义了却从未被代码消费**（grep 确认 0 调用），抢断退化成另一张普通牌。
>
> **教训**：每次加新机制后，用 grep 验证它真的被实现了。

**抢断**打出时立即掷 d20，成功则回合立即结束（对手不得分）。这让它在 DC 很低（对手很可能进）时反而最值——30% 概率一击致命。

**紧逼**的价值不在当回合（只 +2），而在消耗对手体能，为后续回合铺路。

### 6.6 特殊战术牌

| 牌 | 效果 |
|---|---|
| **点哥战术** | 指定对方一名场下球员，扣其 1 点体能并强制其成为本回合防守人 |
| **教练暂停** | 取消对方刚打出的一张战术牌 |

---

## 7. 教练系统

**机制 = 改牌堆构成 + 解锁专属战术牌。** 这是唯一的构筑环节。

| 教练 | 体系 | 牌堆调整 | 解锁 |
|---|---|---|---|
| **泰伦·卢** | 猎杀错位 | 挡拆 +2、单打 +1、无球跑动 -1 | 【中锋上提】×2：强制对手换上体能最低的球员防守 |
| **史蒂夫·科尔** | 传切体系 | 无球跑动 +2、手递手 +1、单打 -1 | 【分球空切】×2：三分 DC -4 |
| **迈克·德安东尼** | 七秒快攻 · 全员换防 | 无限换防 +2、包夹 +1、沉退 -1 | 【全员换防】×2：所有出手 DC +3，免疫错位惩罚 |

三位教练的体系均核实自 2017 年真实历史：

- 卢：派中锋做掩护逼对手换防到詹姆斯身上；G1 詹姆斯发起 1.11 PPP vs 欧文 0.8 PPP
- 科尔："4" Pop Fist / Motion Weak / Loop Punch Stagger Split，复出首场全队 34 次助攻
- 德安东尼："Talk it. Touch it. Switch it. Grab it."，2017 年度最佳教练

---

## 8. 技术架构

### 8.1 文件结构

```
cardgame-design/
├── rules.mjs              ← 规则内核，唯一真相源
├── game.template.html     ← 界面模板（含 /*__RULES__*/ 注入点）
├── build.mjs              ← 把 rules.mjs 内联进模板 → game.html
├── sim.mjs                ← 蒙特卡洛校准模拟器
├── uitest.mjs             ← 浏览器断言测试（91 项）
├── playtest.mjs           ← 自动对局机器人（交付前必过）
├── synctest.mjs           ← 分屏同步验证
├── run.mjs                ← 一键跑全流程
├── game.html              ← 可玩产物（单文件，零依赖）
├── DESIGN.md              ← 开发决策记录
├── README.md              ← 快速上手
└── shots/                 ← 各阶段截图
```

### 8.2 单一真相源原则

**规则只写在 `rules.mjs` 一处**，模拟器与界面都从它读。

```
rules.mjs ──┬──→ sim.mjs（校准：import）
            └──→ build.mjs ──→ game.html（界面：内联注入）
```

`build.mjs` 会校验注入是否成功，避免"模拟的规则"与"实际玩的规则"不一致。

> 早期版本在模拟器和界面里各写了一份常量，导致"模拟器说没问题、实际玩起来不对"。现在物理上不可能不一致。

### 8.3 构建流程

```bash
node build.mjs    # rules.mjs → 内联进 game.template.html → game.html
```

产物 89 KB，单文件自包含，`file://` 下直接双击可运行。

### 8.4 零后端分屏 PVP

两个窗口通过 `BroadcastChannel` 同步，共享 `localStorage` 状态：

```
game.html?side=CLE      # 骑士视角
game.html?side=GSW      # 勇士视角
```

无 `?side=` 的窗口显示"加入"界面，不会冲掉正在进行的比赛。

`file://` 协议下实测可通，**无需起服务器**（`synctest.mjs` 验证过）。

### 8.5 确定性随机

```js
function mulberry32(seed) { /* ... */ }
const d20 = (rng) => 1 + Math.floor(rng() * 20);
```

每次掷骰的种子是 `S.seed * 7919 + S.rollSeq++ * 104729`——种子来自对局，序号递增保证同一局内每次掷骰结果不同，但整局**可复现**（用于调试和回归测试）。

### 8.6 界面布局

```
┌─────────────────────────────────────────────┐
│ 对手球员排（5 张，盲选期整排翻面）              │
├─────────────────────────────────────────────┤
│ 状态栏：回合 / 阶段 / 视角切换 / 重开           │
│ 对战台：双方场上球员 + 打出的牌 + 骰子          │
├──────────────────────┬──────────────────────┤
│ 提示 + 动作按钮        │ 回合流               │
│                      │ 攻防相克速查表         │
├──────────────────────┴──────────────────────┤
│ 我方球员排 + 手牌（进攻/防守/战术三堆，扇形）   │
└─────────────────────────────────────────────┘
```

**交互**：

- 球员卡是竖版真卡牌（291×152）：号码大水印、球队渐变色条、上下分区
- 手牌是竖版扇形（78×114）：轻微旋转叠压、花色符号水印（◆进攻 ▲防守 ✦战术）
- 出牌支持**拖到高亮落点**或直接点击
- 进攻牌面直接印 `需X · Y%`（已含全部修正）
- **DC 拆解摊开给玩家看**：每一项修正来源都能看到

### 8.7 调试桥

页面暴露四个接口用于构造任意局面和逐项拆解数值：

```js
window.__S()          // 当前状态
window.__MY()         // 当前视角（SOLO 模式下不一定等于 URL ?side）
window.__dispatch(a)  // 派发动作
window.__calcDC(shot) // 计算某类出手的 DC
window.__setView(side)// 切换视角
```

---

## 9. 校准数据

4000 局蒙特卡洛 + 8 局真实浏览器对局。

| 指标 | 值 | 目标 |
|---|---|---|
| 得分回合占比 | **65.7%** | 65-78% |
| 每回合得分 | 1.56 | 1.4-1.9（真实 NBA 是 1.10，本作刻意高于它） |
| 一局回合数 | **17**（12-23） | 14-30 |
| 用到球员数 | **4.97 / 5** | ≥3.5 |
| 胜率 | **CLE 49.9% / GSW 50.1%** | 接近 50/50 |
| 命中率区间 | 45% - 90% | 有防守时也要 50-70% |
| 无人可派回合 | **0.00%** | 0% |
| 手上无进攻牌 | **0.01%** | ≈0 |
| 自然 20 / 自然 1 | 4.8% / 5.0% | 各约 5% |

**牌与技能使用率**（验证机制真的在用，不是死代码）：

| 项 | 分布 |
|---|---|
| 防守牌 | 封盖 21.9% / 干扰投篮 20.6% / 抢断 16.5% / 紧逼 9.6% |
| 抢断成功率 | 30%（理论 30%） |
| 出手类型 | 三分 32.5% / 突破 18.6% / 空接 16.9% / 扣篮 16.0% / 中投 15.9% |
| 死亡缠绕触发 | 1.79 次/局 |
| 接管比赛触发 | 0.98 次/局 |

**手牌稳定性**（验证不会枯竭）：

| | 前 7 回合 | 后 7 回合 |
|---|---|---|
| 进攻 / 防守 / 战术 | 3.0 / 3.0 / 2.6 | 3.0 / 3.0 / 3.9 |

---

## 10. 验证流程

```bash
node run.mjs           # 全流程：sim → build → test → play
node run.mjs sim       # 只跑 4000 局校准
node run.mjs build     # 只构建
node run.mjs test      # 只跑浏览器断言
node run.mjs play      # 只跑自动对局机器人
```

**四步必须全绿才能交付。**

### 10.1 为什么必须有自动对局机器人

> **血泪教训**：早期版本 88 项断言全绿，但玩家实际操作两步就卡死。

根因：**断言测试只走脚本设计好的那条路径，走不到玩家真实操作序列的角落**。

`playtest.mjs` 完全通过真实 DOM 点击打完整局，**不调 `__dispatch` 走后门**（否则测不出界面缺按钮）。任何一步找不到可点击项就报卡死，并打出最近 14 步轨迹。

它抓到过的真 bug：

| 症状 | 根因 |
|---|---|
| 发动场下技能后按钮全消失 | "放弃"按钮写成**条件性**的（只在没牌时给） |
| 选完人后界面无出路 | pick 后**没切到等待方视角**，`.seatrow.mine` 仍停在自己这边 |
| 打到后面没牌选 | 起手 7 张 = 上限 7 张，摸了又扔 + 弃牌无脑弃最多的 |

### 10.2 机器人设计要点

```js
// 决策优先级：选人 > 打手牌 > 实质按钮 > 切视角
// 1) 选人必须按 budgetLeft（剩余可上场次数）排序，不能按 DOM 第一个
// 2) 选完人后必须 __setView(whoActs())，否则会卡在"等对方选人"
// 3) next/下一回合 排到最后，保证本回合能做的都做完
// 4) window.__fastDice 把骰子动画从 940ms 降到 30ms，否则一局 50 秒会超时
```

---

## 附：设计原则速查

给未来改动这个项目的人：

1. **规则只写一处**（`rules.mjs`），改动后必须跑 `node run.mjs`
2. **每个阶段都必须有一条无条件存在的出路按钮**——条件性兜底是卡死的头号来源
3. **新机制加完后用 grep 验证它被消费**——定义了但没实现等于不存在
4. **命中率是设计取向不是客观真理**——真实数据只约束相对排序，绝对值服从手感
5. **改数值后连锁检查**：命中率↑ → 回合数↓ → 技能冷却失效 → 要重扫 `targetScore`
6. **用户描述的 bug 几乎从不指向根因**——写诊断脚本把症状拆成结构化数据
7. **断言全绿不代表能玩**——`run.mjs play` 是交付前最后一道关
